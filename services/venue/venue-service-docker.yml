spring:
  boot:
    admin:
      client:
        url: http://admin-server:9090

  cache:
    type: redis

  data:
    redis:
      host: venue-redis
      port: 6371

  datasource:
    url: jdbc:postgresql://venue-postgres-db:54321/venue
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  docker:
    compose:
      enabled: false

eureka:
  instance:
    hostname: discovery-server
    preferIpAddress: true
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

management:
  zipkin:
    tracing:
      endpoint: http://zipkin-server:9411/api/v2/spans
